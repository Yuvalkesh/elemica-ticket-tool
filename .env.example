# Copy to .env and fill in. Never commit .env.
# Stage 1 verifies this file has ANTHROPIC_API_KEY before we build anything.

# --- Anthropic (required for Stage 4 Triage + Stage 5 Resolver) ---
ANTHROPIC_API_KEY=

# --- Server ---
PORT=3000
